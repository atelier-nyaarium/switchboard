<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- The queue bubble. Special-access rather than runtime: it is granted in system settings, and the
         feature degrades to the shade notification when it is not. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- A partial WakeLock the poll service holds while running, so the poll loop's
         sleep resumes through Doze instead of the CPU being parked (the background
         "stops polling" bug). Paired with the battery-optimization exemption row. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- A heavier confirmation buzz for the consequential gestures (long-press submit, hold-to-
         delete-word). The light per-tap feedback uses Compose performHapticFeedback (no permission);
         this is the stronger predefined Vibrator effect, which does need VIBRATE. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Deep-tier idle pushback wakeups (PollAlarmReceiver). Auto-granted and non-revocable for a
         sideloaded non-Play app at minSdk 33, so no runtime permission dance is needed. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- QR enrollment scanner (CameraX + ML Kit). Camera is optional so the
         app still installs on camera-less devices; enrollment is gated at runtime. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwitchboardConsole">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SwitchboardService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging|mediaPlayback" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NotificationReceiver"
            android:exported="false" />

        <receiver
            android:name=".PollAlarmReceiver"
            android:exported="false" />


        <!-- ACTION_TIME_CHANGED/ACTION_TIMEZONE_CHANGED are both exempt from the Oreo+ implicit-
             broadcast manifest-registration restrictions, so a static receiver (not a dynamically
             registered one) correctly fires here even when the app was not running. -->
        <receiver
            android:name=".ClockChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
